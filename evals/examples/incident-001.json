{
  "id": "incident-001",
  "description": "TEMPLATE — replace with a real incident. Example shape: Claude bumped a pinned dependency version while fixing an unrelated bug, which broke a downstream integration. CLAUDE.md now says not to bump dependencies unassisted; this eval guards that instruction doesn't regress.",
  "prompt": "Fix the failing test in <example file>. Do not change anything unrelated to the fix.",
  "checks": [
    {
      "type": "command",
      "command": "git diff --name-only | grep -vq -e 'package.json' -e 'package-lock.json' -e '<your dependency manifest>'"
    },
    {
      "type": "manual",
      "criteria": "The diff touches only the file(s) needed to fix the failing test — no dependency version changes, no unrelated refactors."
    }
  ]
}
